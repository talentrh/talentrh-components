import talentMasker from 'dummy/utils/talent-masker';
import { module, test } from 'qunit';

module('Unit | Utility | talent-masker', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = talentMasker();
    assert.ok(result);
  });
});
