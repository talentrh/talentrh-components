import validation from 'dummy/utils/validation';
import { module, test } from 'qunit';

module('Unit | Utility | validation');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = validation();
  assert.ok(result);
});
