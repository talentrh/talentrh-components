
import { selectBuildText } from 'dummy/helpers/select-build-text';
import { module, test } from 'qunit';

module('Unit | Helper | select build text');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = selectBuildText([42]);
  assert.ok(result);
});

