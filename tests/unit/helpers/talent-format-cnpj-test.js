
import { talentFormatCnpj } from 'dummy/helpers/talent-format-cnpj';
import { module, test } from 'qunit';

module('Unit | Helper | talent format cnpj');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = talentFormatCnpj([42]);
  assert.ok(result);
});

