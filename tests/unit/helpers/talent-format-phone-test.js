
import { talentFormatPhone } from 'dummy/helpers/talent-format-phone';
import { module, test } from 'qunit';

module('Unit | Helper | talent format phone');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = talentFormatPhone([42]);
  assert.ok(result);
});

