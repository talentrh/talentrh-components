
import { getAttribute } from 'dummy/helpers/get-attribute';
import { module, test } from 'qunit';

module('Unit | Helper | get attribute');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getAttribute([42]);
  assert.ok(result);
});

