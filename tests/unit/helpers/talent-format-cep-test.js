
import { talentFormatCep } from 'dummy/helpers/talent-format-cep';
import { module, test } from 'qunit';

module('Unit | Helper | talent format cep');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = talentFormatCep([42]);
  assert.ok(result);
});

