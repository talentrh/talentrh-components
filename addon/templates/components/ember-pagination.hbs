<nav aria-label="Page navigation">
		<ul class="pagination justify-content-center">
      {{#if havePrev}}
        <li  {{action 'prev'}} class="page-item">
          <a class="page-link" href="#" tabindex="-1">Anteriores</a>
        </li>
      {{/if}}

      {{#each arrayPages as |page| }}
        <li {{action 'loadNewPage' page}} class="page-item">
          <a class="page-link" href="#">{{page.pageNumber}}</a>
        </li>
      {{/each}}

      {{#if haveNext}}
        <li class="page-item" {{action 'next'}}>
          <a class="page-link" href="#">Próximas</a>
        </li>
      {{/if}}
		</ul>
	</nav>
